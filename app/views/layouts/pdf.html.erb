<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Reporte Solara PDF</title>
  <style>
    /* Estilos optimizados para PDF */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      font-size: 12px;
      line-height: 1.4;
      color: #333;
    }

    .header {
      background: linear-gradient(135deg, #2563eb, #1d4ed8);
      color: white;
      padding: 20px;
      text-align: center;
      margin-bottom: 20px;
    }

    .header h1 {
      font-size: 24px;
      margin-bottom: 5px;
    }

    .header p {
      font-size: 14px;
      opacity: 0.9;
    }

    .section {
      margin-bottom: 25px;
      page-break-inside: avoid;
    }

    .section h2 {
      font-size: 18px;
      color: #2563eb;
      border-bottom: 2px solid #e2e8f0;
      padding-bottom: 5px;
      margin-bottom: 15px;
    }

    .summary-grid {
      display: table;
      width: 100%;
      margin-bottom: 20px;
    }

    .summary-row {
      display: table-row;
    }

    .summary-cell {
      display: table-cell;
      padding: 10px;
      border: 1px solid #e2e8f0;
      text-align: center;
      vertical-align: middle;
    }

    .summary-header {
      background: #f8fafc;
      font-weight: bold;
      font-size: 10px;
      text-transform: uppercase;
    }

    .summary-value {
      font-size: 14px;
      font-weight: bold;
    }

    .positive { color: #16a34a; }
    .negative { color: #dc2626; }
    .warning { color: #f59e0b; }
    .neutral { color: #1e293b; }

    .systems-table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }

    .systems-table th,
    .systems-table td {
      padding: 8px;
      border: 1px solid #e2e8f0;
      text-align: left;
    }

    .systems-table th {
      background: #f8fafc;
      font-weight: bold;
      font-size: 10px;
      text-transform: uppercase;
    }

    .systems-table td {
      font-size: 11px;
    }

    .footer {
      position: fixed;
      bottom: 0;
      width: 100%;
      text-align: center;
      font-size: 10px;
      color: #64748b;
      padding: 10px;
      border-top: 1px solid #e2e8f0;
    }

    .page-break {
      page-break-before: always;
    }
  </style>
</head>
<body>
  <%= yield %>

  <div class="footer">
    🌞 Solara - Sistemas Fotovoltaicos | Generado: <%= Time.current.strftime("%d/%m/%Y %H:%M") %>
  </div>
</body>
</html>
